{
  "name": "check-vaccine-slot",
  "version": "1.0.0",
  "description": "Node file to fetch available vaccine slots",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf dist/ && babel ./ --out-dir dist/ --ignore ./node_modules,./.babelrc,./package.json,./npm-debug.log --copy-files",
    "start": "node ."
  },
  "keywords": [
    "vaccine",
    "covid-19",
    "slots"
  ],
  "author": "Ashiv Gupta",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.21.1",
    "commander": "^7.2.0"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "rimraf": "^3.0.2"
  }
}
